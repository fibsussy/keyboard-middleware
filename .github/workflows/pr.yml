name: Build to test PR

on:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        os: [linux]
        arch: [x86_64, aarch64]
    steps:
      - uses: actions/checkout@v4
      - name: Install cross
        run: cargo install cross --locked
      - name: Clean build artifacts
        run: cargo clean
      - name: Build binary with cross
        run: cross build --release --target ${{ matrix.arch }}-unknown-${{ matrix.os }}-gnu
      - name: Create tarball
        run: |
          mv target/${{ matrix.arch }}-unknown-${{ matrix.os }}-gnu/release/keymux keymux
          tar -czf keymux-${{ matrix.os }}-${{ matrix.arch }}.tar.gz keymux
      - name: Generate checksum
        run: sha256sum keymux-${{ matrix.os }}-${{ matrix.arch }}.tar.gz > keymux-${{ matrix.os }}-${{ matrix.arch }}.sha256
      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: keymux-${{ matrix.os }}-${{ matrix.arch }}
          path: |
            keymux-${{ matrix.os }}-${{ matrix.arch }}.tar.gz
            keymux-${{ matrix.os }}-${{ matrix.arch }}.sha256
